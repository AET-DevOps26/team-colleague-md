# Python caches and local artifacts — keep them out of the COPY . . build context.
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.git/
*.egg-info/
.coverage
htmlcov/

# Never bake local secrets into the image.
.env
