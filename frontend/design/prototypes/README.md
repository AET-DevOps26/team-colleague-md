# Prototypes

React experiments and interactive prototypes go here.

This folder is reserved for working code (component sketches, hook trials, integration tests against the design system) that goes beyond the static HTML demos in `../animations/`.
