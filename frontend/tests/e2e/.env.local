# Local docker compose stack — this is what the suite targets when no BASE_URL is set, so you only
# need this file if you want to be explicit.
#
#   docker compose up -d && npm run seed:local   (from the repo root)
#   cd frontend && npm test
BASE_URL=http://localhost:3000
SEED_PASSWORD=Password123!
