# verita-dev on Rancher. Seed it first — see docs/infrastructure/Seeding_Remote_Environments.md.
#
#   set -a && . tests/e2e/.env.verita-dev && set +a && npm test
#
# The suite writes to the target (posts, comments, follows, settings), so expect it to leave seed
# accounts touched. Every spec restores what it changes, but this is still a shared environment.
BASE_URL=https://dev.verita.stud.k8s.aet.cit.tum.de
SEED_PASSWORD=Password123!
