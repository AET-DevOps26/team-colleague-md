package com.verita.contentservice.entity;

/** Whether a digest is personalised for one subscriber or shared across the platform (ADR-0019). */
public enum DigestTypeValue {
    PERSONAL,
    PUBLIC
}
