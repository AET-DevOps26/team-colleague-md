spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dev-only fallback — never use in production; production must set JWT_SECRET env var
app.jwt-secret=dev-only-insecure-secret-replace-in-production!!
