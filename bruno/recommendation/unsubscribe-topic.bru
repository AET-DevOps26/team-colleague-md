meta {
  name: Unsubscribe from topic
  type: http
  seq: 4
}

delete {
  url: {{recBaseUrl}}/api/v1/subscriptions/topics/{{topicId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
