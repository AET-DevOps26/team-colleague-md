meta {
  name: Unsubscribe from user
  type: http
  seq: 7
}

delete {
  url: {{recBaseUrl}}/api/v1/subscriptions/users/{{otherUserId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
