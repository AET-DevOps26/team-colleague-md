meta {
  name: Update current user
  type: http
  seq: 6
}

put {
  url: {{userBaseUrl}}/api/v1/users/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "displayName": "Updated Name",
    "bio": "Updated via Bruno"
  }
}
