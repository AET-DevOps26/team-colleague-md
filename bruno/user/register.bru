meta {
  name: Register
  type: http
  seq: 2
}

post {
  url: {{userBaseUrl}}/api/v1/auth/register
  body: json
  auth: none
}

body:json {
  {
    "username": "newuser",
    "email": "newuser@example.com",
    "password": "{{userPassword}}"
  }
}
