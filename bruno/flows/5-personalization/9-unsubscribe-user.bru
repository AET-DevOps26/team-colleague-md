meta {
  name: Unsubscribe user (cleanup)
  type: http
  seq: 9
}

delete {
  url: {{recBaseUrl}}/api/v1/subscriptions/users/{{otherUserId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: lt 300
}
