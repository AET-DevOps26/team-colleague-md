meta {
  name: Subscribe to topic
  type: http
  seq: 4
}

post {
  url: {{recBaseUrl}}/api/v1/subscriptions/topics/{{topicId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: lt 300
}
