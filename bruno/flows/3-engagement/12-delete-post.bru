meta {
  name: Delete post (cleanup)
  type: http
  seq: 12
}

delete {
  url: {{contentBaseUrl}}/api/v1/posts/{{postId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: lt 300
}
