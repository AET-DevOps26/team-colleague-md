meta {
  name: Unbookmark (cleanup)
  type: http
  seq: 10
}

delete {
  url: {{contentBaseUrl}}/api/v1/posts/{{postId}}/bookmark
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: lt 300
}
