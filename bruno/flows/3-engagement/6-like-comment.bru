meta {
  name: Like comment
  type: http
  seq: 6
}

post {
  url: {{contentBaseUrl}}/api/v1/comments/{{commentId}}/like
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: lt 300
}
