meta {
  name: Delete comment (cleanup)
  type: http
  seq: 11
}

delete {
  url: {{contentBaseUrl}}/api/v1/comments/{{commentId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: lt 300
}
