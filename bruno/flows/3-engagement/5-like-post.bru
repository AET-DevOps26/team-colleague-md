meta {
  name: Like post
  type: http
  seq: 5
}

post {
  url: {{contentBaseUrl}}/api/v1/posts/{{postId}}/like
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: lt 300
}
