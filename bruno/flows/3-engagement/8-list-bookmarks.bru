meta {
  name: List bookmarks
  type: http
  seq: 8
}

get {
  url: {{contentBaseUrl}}/api/v1/users/{{ownUserId}}/bookmarks
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}
