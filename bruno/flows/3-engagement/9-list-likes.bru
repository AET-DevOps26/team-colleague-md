meta {
  name: List likes
  type: http
  seq: 9
}

get {
  url: {{contentBaseUrl}}/api/v1/users/{{ownUserId}}/likes
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}
