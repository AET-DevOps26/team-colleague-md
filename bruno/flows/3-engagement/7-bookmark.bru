meta {
  name: Bookmark post
  type: http
  seq: 7
}

post {
  url: {{contentBaseUrl}}/api/v1/posts/{{postId}}/bookmark
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: lt 300
}
