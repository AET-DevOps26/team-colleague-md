meta {
  name: Get preferences
  type: http
  seq: 5
}

get {
  url: {{userBaseUrl}}/api/v1/users/me/preferences
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: eq 200
}
