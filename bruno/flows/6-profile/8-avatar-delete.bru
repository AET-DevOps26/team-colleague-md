meta {
  name: Delete avatar (cleanup)
  type: http
  seq: 8
}

delete {
  url: {{userBaseUrl}}/api/v1/users/me/avatar
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

assert {
  res.status: lt 300
}
