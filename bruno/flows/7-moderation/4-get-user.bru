meta {
  name: Get user by id
  type: http
  seq: 4
}

get {
  url: {{userBaseUrl}}/api/v1/users/{{otherUserId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq {{otherUserId}}
}
