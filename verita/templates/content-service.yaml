apiVersion: v1
kind: Service
metadata:
  name: {{ include "verita.fullname" . }}
  labels:
    {{- include "verita.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "verita.selectorLabels" . | nindent 4 }}
